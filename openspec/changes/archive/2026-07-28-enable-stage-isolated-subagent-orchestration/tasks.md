## 1. 先建立失败测试与治理契约

- [x] 1.1 在 `scripts/test-handoff-contracts.py` 先编写 RED 测试：用临时 change/合同夹具覆盖四类 schema 的必填字段与枚举、未知字段、项目根相对路径与 traversal/symlink 越界、权威输入 fingerprint freshness、请求/结果/review fingerprint 关联、`findingId` 唯一性、correction→复核关闭、仅主 Agent可持久化，以及未来新 change 使用非空 `taskIds: ["BOOTSTRAP-PRODUCT-SPEC"]` 的 bootstrap `PRODUCT_SPEC` 合同创建后立即落盘；同时验证本能力 rollout 的 pre-capability `NOT_APPLICABLE` 不允许追溯伪造合同；预期结果是实现前因 schema/validator 不存在而失败，验证命令为 `python3 scripts/test-handoff-contracts.py`。
- [x] 1.2 在 `scripts/test-ai-delivery-governance.py` 先增加角色路由与治理引用 RED 测试：精确校验七个可委派合同阶段分别只引用现有七角色，`EVIDENCE_AND_ARCHIVE` 仅作为主 Agent控制状态且不签发 specialist 合同，主 Agent不登记为第八角色，并校验进入/退出条件、写入根、QA/前后端/reviewer 并行约束，以及 Skill/Rule/README/manifest/lock/required-files 对新资源的引用；预期结果是缺少新矩阵与引用时失败，验证命令为 `python3 scripts/test-ai-delivery-governance.py`。
- [x] 1.3 在 `scripts/test-custom-agents.py` 和 `scripts/validate-custom-agents.py` 先增加结构化交接失败夹具与稳定标记校验，覆盖 specialist 必须消费已验收合同、返回对应结果类型、不得生成用户确认或自行派工、只读 reviewer 不扩权；预期结果是任一 TOML 缺失合同责任或越界时指出角色名并失败，验证命令为 `python3 scripts/test-custom-agents.py` 与 `python3 scripts/validate-custom-agents.py`。
- [x] 1.4 在 `scripts/test-handoff-contracts.py` 与 `scripts/test-ai-delivery-governance.py` 增加控制面 self-hosting RED 夹具：有效链必须是 `CONTROL_PLANE_IMPLEMENTATION + main_agent + CONTROL_PLANE` request→真实 result→`QA/qa_engineer` 与 `SPEC_REVIEW/spec_reviewer` ReviewResult；混用任一标记、把控制面用于 `ReviewResult`/体验审查、缺少已满足规格 gate、使用宽泛目录、allowlist 外路径、result 身份或 modifiedFiles 不匹配、控制面 finding/correction 断链均必须失败，同时继续断言只有七个 custom agents；预期结果是实现前失败且不通过伪造 specialist 修复，验证命令为 `python3 scripts/test-handoff-contracts.py` 与 `python3 scripts/test-ai-delivery-governance.py`。

## 2. 实现版本化合同 Schema 与 Validator

- [x] 2.1 新建 `.codex/skills/chinamate-fullstack-delivery/schemas/common.schema.json`、`task-contract.schema.json`、`result-contract.schema.json`、`review-result.schema.json`、`correction-contract.schema.json`，实现 design 中 `schemaVersion = 1.0`、公共身份字段、七个可委派合同阶段/七角色/执行模式/状态/gate/finding 枚举、非空 `taskIds` 及保留值 `BOOTSTRAP-PRODUCT-SPEC`，并为 Task/Result/Correction 增加互斥的 `CONTROL_PLANE_IMPLEMENTATION + main_agent` 分支，其中 Task 固定 `executionMode = CONTROL_PLANE`；`ReviewResult` 与 specialist 路由枚举继续排除控制面身份和 `EVIDENCE_AND_ARCHIVE` 并收紧未知字段；预期结果是四类有效最小夹具和受限控制面实现链通过，空 taskIds、未知枚举、混合身份或主 Agent控制状态被用于 specialist/review 合同的夹具失败，验证命令为 `python3 scripts/test-handoff-contracts.py`。
- [x] 2.2 新建 `.codex/skills/chinamate-fullstack-delivery/scripts/validate_handoff_contract.py`，只使用 Python 标准库读取上述 schema 并实现项目所需关键字与可判定交叉检查，不新增 `jsonschema`、npm package 或其他未经批准的运行时依赖；校验路径规范化和角色授权根、控制面精确文件 allowlist/规格确认 gate/三字段联合判别、request-result 身份与 modifiedFiles 子集、SHA-256 freshness、控制面 result→QA/Spec Review fingerprint、finding/correction/复核闭环及持久化者标记，并以非零退出码和字段路径报告失败；预期结果是有效合同 `PASS`、stale 返回 `STALE`、越界、混用、伪造 review 输入与断链返回 `FAIL`，验证命令为 `python3 scripts/test-handoff-contracts.py` 及对最小有效夹具执行 validator CLI。
- [x] 2.3 新建 `.codex/skills/chinamate-fullstack-delivery/references/handoff-contracts.md`，只说明四类合同字段、`handoffs/<contractId>/` 布局、主 Agent唯一持久化责任、未来新 change 创建前使用 `BOOTSTRAP-PRODUCT-SPEC` 的 `PRODUCT_SPEC` bootstrap JSON 合同及创建后立即落盘流程、本能力 rollout 的 pre-capability `NOT_APPLICABLE` 边界、受限控制面合同固定身份/精确 allowlist/真实 Result→QA/Spec Review 链、fingerprint 重签/重新验收、事实源边界和最小脱敏示例，不复制 OpenSpec/Rules 正文；预期结果是下游无需聊天摘要即可构造 specialist 或控制面合同，且不会追溯伪造本 change 规划或实现快照，验证方式为 `python3 scripts/test-ai-delivery-governance.py` 检查引用与示例 JSON 可解析。

## 3. 接入阶段路由、fresh subagent 与 review/correction 闭环

- [x] 3.1 新建 `.codex/skills/chinamate-fullstack-delivery/references/role-routing.md`，登记任务类型、七个可委派合同阶段、现有七角色、进入/退出条件、默认执行模式、允许并行对象和写入根，并把 `EVIDENCE_AND_ARCHIVE` 单列为无 specialist 合同的主 Agent控制状态；在独立控制面表中登记 `CONTROL_PLANE_IMPLEMENTATION + main_agent + CONTROL_PLANE` 的已确认规格 gate、精确文件 allowlist 和禁止职责，不把它加入 specialist 路由集合；同时更新 `references/stage-routing.md` 与 `references/control-matrix.md`，表达前后端冻结后可并行、QA 等待稳定实现、两个只读 reviewer 按适用性可并行、控制面 result 必须交给现有 QA/Spec Reviewer、主 Agent唯一签发/验收及用户/Git硬关卡；预期结果是矩阵仍恰好七个 specialist，不新增 `code_reviewer`、第八角色或 evidence/archive specialist 合同，验证命令为 `python3 scripts/test-ai-delivery-governance.py`。
- [x] 3.2 更新 `.codex/skills/chinamate-fullstack-delivery/SKILL.md`，将默认串行职责视角替换为控制面签发合同和按工作包创建 fresh subagent，接入 `handoff-contracts.md`、`role-routing.md` 与 validator；明确同一合同 correction 可复用原 subagent、合同关闭后新工作包 fresh、未来 bootstrap 使用 `BOOTSTRAP-PRODUCT-SPEC`、`EVIDENCE_AND_ARCHIVE` 不签发 specialist 合同、fast path/`DEGRADED`/`BLOCKED`、共享工作区流程拒收语义，以及只在无 specialist 归属的已确认根治理实现中允许受限控制面合同并强制后续 QA/Spec Review；预期结果是 Skill 不维护第二状态、不自动 Git/归档、不把控制面例外用于业务、应用实现、QA 或 review，也不伪造多 Agent交付，验证命令为 `python3 scripts/test-ai-delivery-governance.py`。
- [x] 3.3 更新 `.codex/rules/workflow.md` 中 `RULE-WF-005`、根 `AGENTS.md` 和 `.codex/agents/README.md`，把长期约束与入口路由改为“最少必要 specialist subagent + 结构化合同”，并保持规格确认、权限和证据门禁；预期结果是三处无“主 Agent默认串行模拟七职责”的冲突表述，验证命令为 `./scripts/check-agent-governance.sh`。
- [x] 3.4 更新 `.codex/agents/product_manager.toml`、`interaction_designer.toml`、`frontend_engineer.toml`、`backend_engineer.toml`、`qa_engineer.toml`、`spec_reviewer.toml`、`experience_reviewer.toml`，为每个角色补充对应 `TaskContract` 输入、`ResultContract`/`ReviewResult` 输出、允许/禁止写范围、finding/correction 责任以及“不得生成用户确认、自行派工或直接写 change 根 `handoffs/`”；预期结果是七角色只返回结构化 payload，仍保持原 sandbox 和业务职责，验证命令为 `python3 scripts/validate-custom-agents.py` 与 `python3 scripts/test-custom-agents.py`。

## 4. 接入文档、Manifest、锁与根治理入口

- [x] 4.1 更新 `README.md` 的 Agent/Rules/Skills 治理说明，记录主 Agent控制面、fresh/reuse、结构化合同、并行边界、fast path/受控降级、受限控制面 self-hosting 例外及 `handoffs/` 非事实源定位；预期结果是人类入口与 Skill/Rule 一致，明确控制面标记不是第八 custom agent 且不能替代业务 specialist、QA 或 review，并且不承诺七角色全启或物理隔离，验证命令为 `python3 scripts/test-ai-delivery-governance.py`。
- [x] 4.2 更新 `.codex/manifest.json`、`.codex/skills-lock.json` 和 `scripts/check-agent-governance.sh` 的 first-party Skill 资源登记/required files/测试入口，并按现有确定性 `project_skill_hash` 算法同步 `chinamate-fullstack-delivery` 的 `computedHash` 与 `contentHash`；预期结果是新 schema、references、validator 与测试均被治理入口发现，仍只登记七个 custom agents，验证命令为 `python3 scripts/test-ai-delivery-governance.py` 与 `./scripts/check-agent-governance.sh`。

## 5. 用真实合同快照完成本 change 的闭环证据

- [x] 5.1 在用户确认本次规格更新且控制面 schema/validator 支持生效后，由主 Agent先签发只覆盖剩余根治理增量的 `CONTROL_PLANE_IMPLEMENTATION + main_agent + CONTROL_PLANE` request，再执行真实工作并返回与 request fingerprint 关联的控制面 `ResultContract`；随后分别签发 `QA/qa_engineer` 与 `SPEC_REVIEW/spec_reviewer` 合同，使两个 `ReviewResult.resultFingerprint` 引用该控制面结果，适用 finding 使用不扩范围的控制面 correction 并由原 reviewer 复核关闭。主 Agent作为唯一持久化写入者，在 `openspec/changes/enable-stage-isolated-subagent-orchestration/handoffs/<contractId>/` 校验并保存首批真实脱敏 `request.json`、`result.json`、`reviews/*.json`、`corrections/*.json`；不得把控制面合同用于 QA/review，不得为本 change 已完成的 `PRODUCT_SPEC` 或合同签发前实现追溯创建 request/result；预期结果是真实 Result→QA/Spec Review 链只引用已验收合同和当前权威输入，输入变化后旧快照被判 `STALE` 并重签或重新验收，验证命令为 `python3 .codex/skills/chinamate-fullstack-delivery/scripts/validate_handoff_contract.py --change enable-stage-isolated-subagent-orchestration --all`。
- [x] 5.2 使用 `docs/templates/openspec-change-evidence.md` 新建并维护 `openspec/changes/enable-stage-isolated-subagent-orchestration/evidence.md`，把本 change 产品规划阶段记录为 pre-capability structured assignment、合同持久化 `NOT_APPLICABLE`，并汇总 schema/validator 生效后的真实开发、QA、Spec Review、适用性判断、失败/阻塞/未运行与残余风险；只链接真实 handoff 快照，不补造规划阶段合同；预期结果是证据可追溯且不含追溯伪造、原始日志、凭据或虚假用户/Git授权，验证方式为人工脱敏复核和 `./scripts/check-agent-governance.sh`。

## 6. 最终验证与交接

- [x] 6.1 运行 `python3 scripts/test-handoff-contracts.py`、`python3 scripts/test-custom-agents.py`、`python3 scripts/test-ai-delivery-governance.py`、`./scripts/check-agent-governance.sh` 和 `./scripts/check-harness.sh`；预期结果是成功夹具全部通过、缺字段/越权/stale/finding断链/引用漂移失败夹具均准确失败，并明确机器门禁未验证自然语言语义。
- [x] 6.2 运行 `openspec validate enable-stage-isolated-subagent-orchestration --strict`、`openspec validate --all --strict` 和 `git diff --check`，复核只修改根治理、OpenSpec 与文档范围且 `frontend/`、`backend/` 业务实现不变；预期结果是 OpenSpec strict 与 diff 检查通过，任何环境阻塞或未运行项如实写入 `evidence.md`。
- [x] 6.3 由 `spec_reviewer` 基于已验收的真实控制面 `ResultContract` 只读执行 proposal/design/tasks/specs 与实际 diff 的双向对账，并由主 Agent复核该 `ReviewResult.resultFingerprint`、所有 `OPEN` 阻断 finding、freshness、用户/Git gates 和残余风险；预期结果是 reviewer 不使用控制面身份、不修改规格或代码，满足条件时只提出同步/归档或 Git 操作建议，未经用户明确授权不执行 commit、push、gitlink 更新或 archive，验证依据为 `ReviewResult` 与 `evidence.md`。
